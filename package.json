{
  "name": "netlify-identity-members-starter",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=20.12.2"
  },
  "scripts": {
    "dev": "npm run build:dashboard && netlify dev",
    "build": "npm run build:dashboard && node scripts/run-tests.cjs",
    "build:dashboard": "node scripts/build-dashboard.cjs",
    "watch:dashboard": "node scripts/build-dashboard.cjs --watch",
    "pretest": "npm run build:dashboard",
    "test": "node scripts/run-tests.cjs",
    "start": "npm run dev"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "jsdom": "26.1.0",
    "netlify-cli": "^26.2.0"
  },
  "dependencies": {
    "@netlify/blobs": "10.7.9",
    "react": "19.3.0",
    "react-dom": "19.3.0",
    "stripe": "22.3.2"
  }
}
